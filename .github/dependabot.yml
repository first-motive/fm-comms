version: 2

updates:
  # GitHub Actions workflow dependencies.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly

  # Python dependencies. Adjust package-ecosystem to match the repo
  # (pip covers uv/python projects); remove this block if not applicable.
  - package-ecosystem: pip
    directory: /
    schedule:
      interval: weekly
